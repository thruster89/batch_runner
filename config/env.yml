oracle:
  thick:
    # Oracle Instant Client 경로 (Thick mode)
    instant_client: "C:\\Users\\thrus\\Downloads\\WINDOWS.X64_213000_db_home1\\bin"

  # 기본 실행 대상 host (CLI --hosts로 override 가능)
  run:
    hosts:
      - host1
      # - host2

  # host별 Oracle 접속 정보
  hosts:
    host1:
      dsn: ORCLPDB                # tnsnames.ora alias 또는 EZCONNECT
      user: tester
      password: "aa12345"
      duckdb_schema: SRC_HOST1

    host2:
      dsn: IFRSDB2
      user: IFRS_USER
      password: IFRS_PASSWORD
      duckdb_schema: SRC_HOST2